import Link from "next/link";

const Button = (Props: any) => {
  return (
    <>
      {/* {type === Button && (
        <button className="btn btn-outline-primary-dark">Button</button>
      )}{" "} */}
      {<Link href="">This is link</Link>}
    </>
  );
};
export default Button;
